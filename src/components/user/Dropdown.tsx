"use client";

import { Session } from "next-auth";

interface Props {
    session: Session
}

export default function UserDropdown({ session }: Props) {
    return (
        <>
            
        </>
    )
}