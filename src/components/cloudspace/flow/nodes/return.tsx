"use client";

import type { ReturnNode } from "@/types/koxy";
import { NodeProps } from "./types";

export default function ReturnNode({}: NodeProps<ReturnNode>) {

    return (
        <>return node</>
    )

}